<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#ffffffff">
    <include layout="@layout/oe" />
    <LinearLayout
        android:id="@id/xd"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/xx">
        <FrameLayout
            android:id="@id/xe"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content" />
        <include layout="@layout/od" />
    </LinearLayout>
    <WebView
        android:id="@id/xi"
        android:layout_width="fill_parent"
        android:layout_height="0.0dip"
        android:layout_margin="8.0dip"
        app:layout_constraintBottom_toBottomOf="apv"
        app:layout_constraintTop_toBottomOf="@id/xd" />
    <LinearLayout
        android:id="@id/xf"
        android:layout_width="fill_parent"
        android:layout_height="0.0dip"
        android:background="@color/pu"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/xi"
        app:layout_constraintTop_toTopOf="@id/xi">
        <TextView
            android:id="@id/xh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/fcm_notification_detail_retry_message"
            android:textColor="@color/px"
            android:textSize="@dimen/ik" />
    </LinearLayout>
    <ProgressBar
        android:id="@id/xg"
        android:layout_width="@dimen/fm"
        android:layout_height="@dimen/fm"
        android:theme="@style/if"
        app:layout_constraintBottom_toBottomOf="@id/xi"
        app:layout_constraintLeft_toLeftOf="@id/xi"
        app:layout_constraintRight_toRightOf="@id/xi"
        app:layout_constraintTop_toTopOf="@id/xi" />
</androidx.constraintlayout.widget.ConstraintLayout>